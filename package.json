{
  "name": "sign3d-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && NODE_OPTIONS=--openssl-legacy-provider npm run dev",
    "vercel-build": "cd client && npm install && npm run build"
  }
}
